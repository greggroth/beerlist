<div id="follow_form">
	<% if user_signed_in? %>
		<% if bar.is_followed? current_user %>
			<%= render 'unfollow', :bar => bar %>
		<% else %>
			<%= render 'follow', :bar => bar %>
		<% end %>
	<% end %>
</div>
