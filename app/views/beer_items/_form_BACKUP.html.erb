<%= form_for(@beer_item) do |f| %>
  <% if @beer_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_item.errors.count, "error") %> prohibited this beer_item from being saved:</h2>

      <ul>
      <% @beer_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.collection_select(:beer, :id, Beer.all, :id, :name) %><br />
  </div>

  <div class="field">
    <%= f.label :beer %><br />
    <%= f.text_field :beer %>
  </div>
  <div class="field">
    <%= f.label :bar %><br />
    <%= f.text_field :bar %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
