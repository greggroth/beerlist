<ul class="rounded">
	<% for beer in @beers %>
		<li class="arrow"><a class="button_beers_show" data-beer-id="<%= beer.id %>" data-beername="<%= beer.name %>"><%= beer.name %></a></li>
	<% end %>
</ul>
		